package mysmax.com.retrofitapplication.eventbus;

public class BottomLayoutEvent {
    public boolean isExpanded = false;

    public BottomLayoutEvent(boolean isExpanded) {
        this.isExpanded = isExpanded;
    }
}
